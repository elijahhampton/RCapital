const initialState = {}

const investReducer = (initialState : Object, state : ReduxAction) => {
    switch(state.action) {
        default:
            return state
    }
}

export default investReducer;